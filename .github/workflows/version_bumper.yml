---
name: Check for new upstream releases
on:
  schedule:
    - cron: '21 14 * * *'
  workflow_call:
    inputs:
      role-repos:
        required: true
        type: string
jobs:
  discover-role-repos:
    runs-on: ubuntu-latest
    outputs:
      role-repos: ${{ steps.discover.outputs.result }}
    container:
      image: quay.io/prometheus/golang-builder:base
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0

      - name: Get repos for each role
        id: discover
        run: ./.github/scripts/discover_role_repos.sh | tee -a "$GITHUB_OUTPUT"

  bump_role:
    runs-on: ubuntu-latest
    needs: discover-role-repos
    permissions:
      contents: write
      pull-requests: write
    container:
      image: quay.io/prometheus/golang-builder:base
    env:
      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    strategy:
      matrix:
        include: ${{ fromJson(needs.discover-role-repos.outputs.role-repos) }}
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0
      - run: ./.github/scripts/version_updater.sh ${{ matrix.repo }} ${{ matrix.role }} ${{ matrix.type }}
