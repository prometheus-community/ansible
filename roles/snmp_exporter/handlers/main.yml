---
- name: reload snmp exporter
  ansible.builtin.systemd:
    daemon_reload: true
    name: snmp_exporter
    state: reloaded

- name: restart snmp exporter
  ansible.builtin.systemd:
    daemon_reload: true
    name: snmp_exporter
    state: restarted
