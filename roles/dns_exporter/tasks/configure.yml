---
- name: Configure
  ansible.builtin.include_role:
    name: prometheus.prometheus._common
    tasks_from: configure.yml
  vars:
    _common_system_user: "{{ dns_exporter_system_user }}"
    _common_system_group: "{{ dns_exporter_system_group }}"
    _common_config_dir: "{{ dns_exporter_config_dir }}"
    _common_tls_server_config: "{{ dns_exporter_tls_server_config }}"
    _common_http_server_config: "{{ dns_exporter_http_server_config }}"
    _common_basic_auth_users: "{{ dns_exporter_basic_auth_users }}"
  tags:
    - dns_exporter
    - configure
    - dns_exporter_configure

- name: Create textfile collector dir
  ansible.builtin.file:
    path: "{{ dns_exporter_textfile_dir }}"
    state: directory
    owner: "{{ dns_exporter_system_user }}"
    group: "{{ dns_exporter_system_group }}"
    mode: u+rwX,g+rwX,o=rX
  become: true
  when: dns_exporter_textfile_dir | length > 0
  tags:
    - dns_exporter
    - configure
    - dns_exporter_configure
