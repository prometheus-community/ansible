---
- name: Preflight
  ansible.builtin.include_tasks:
    file: preflight.yml
  tags:
    - elasticsearch_exporter_install
    - elasticsearch_exporter_configure
    - elasticsearch_exporter_run

- name: Install
  ansible.builtin.include_role:
    name: prometheus.prometheus._common
    tasks_from: install.yml
  vars:
    _common_local_cache_path: "{{ elasticsearch_exporter_local_cache_path }}"
    _common_binaries: "{{ _elasticsearch_exporter_binaries }}"
    _common_binary_install_dir: "{{ elasticsearch_exporter_binary_install_dir }}"
    _common_binary_url: "{{ elasticsearch_exporter_binary_url }}"
    _common_checksums_url: "{{ elasticsearch_exporter_checksums_url }}"
    _common_system_group: "{{ elasticsearch_exporter_system_group }}"
    _common_system_user: "{{ elasticsearch_exporter_system_user }}"
    _common_config_dir: "{{ elasticsearch_exporter_config_dir }}"
    _common_binary_unarchive_opts: ['--strip-components=1']
  tags:
    - elasticsearch_exporter_install

- name: SELinux
  ansible.builtin.include_role:
    name: prometheus.prometheus._common
    tasks_from: selinux.yml
  vars:
    _common_selinux_port: "{{ elasticsearch_exporter_web_listen_address | urlsplit('port') }}"
  when: ansible_selinux.status == "enabled"
  tags:
    - elasticsearch_exporter_configure

- name: Configure
  ansible.builtin.include_tasks:
    file: configure.yml
  tags:
    - elasticsearch_exporter_configure

- name: Ensure Node Exporter is enabled on boot
  become: true
  ansible.builtin.systemd:
    daemon_reload: true
    name: elasticsearch_exporter
    enabled: true
    state: started
  when:
    - not ansible_check_mode
  tags:
    - elasticsearch_exporter_run
