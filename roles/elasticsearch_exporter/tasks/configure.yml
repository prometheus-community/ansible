---
- name: Configure
  ansible.builtin.include_role:
    name: prometheus.prometheus._common
    tasks_from: configure.yml
  vars:
    _common_system_user: "{{ elasticsearch_exporter_system_user }}"
    _common_system_group: "{{ elasticsearch_exporter_system_group }}"
    _common_config_dir: "{{ elasticsearch_exporter_config_dir }}"
    _common_tls_server_config: "{{ elasticsearch_exporter_tls_server_config }}"
    _common_http_server_config: "{{ elasticsearch_exporter_http_server_config }}"
    _common_basic_auth_users: "{{ elasticsearch_exporter_basic_auth_users }}"
  tags:
    - elasticsearch_exporter
    - configure
    - elasticsearch_exporter_configure

- name: Create textfile collector dir
  ansible.builtin.file:
    path: "{{ elasticsearch_exporter_textfile_dir }}"
    state: directory
    owner: "{{ elasticsearch_exporter_system_user }}"
    group: "{{ elasticsearch_exporter_system_group }}"
    mode: u+rwX,g+rwX,o=rX
  become: true
  when: elasticsearch_exporter_textfile_dir | length > 0
  tags:
    - elasticsearch_exporter
    - configure
    - elasticsearch_exporter_configure
