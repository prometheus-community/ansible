---
_pve_exporter_repo: "prometheus-pve/prometheus-pve-exporter"
_github_api_headers: "{{ {'GITHUB_TOKEN': lookup('ansible.builtin.env', 'GITHUB_TOKEN')} if (lookup('ansible.builtin.env', 'GITHUB_TOKEN')) else {} }}"
_pve_exporter_pkgs:
  - "python{{ ansible_facts['python']['version']['major'] }}-setuptools"
  - "python{{ ansible_facts['python']['version']['major'] }}-venv"
